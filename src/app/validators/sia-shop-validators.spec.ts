import { SiaShopValidators } from './sia-shop-validators';

describe('SiaShopValidators', () => {
  it('should create an instance', () => {
    expect(new SiaShopValidators()).toBeTruthy();
  });
});
